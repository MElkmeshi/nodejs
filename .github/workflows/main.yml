name: remote ssh command
on: [push]
jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: executing remote ssh commands using password
        uses: appleboy/ssh-action@v0.1.8
        with:
          host: host
          username: root
          password: password
          port: 22
          script: bash /home/melkmeshi/nodejs/update.sh
